    <div class="row underline-row" ng-repeat="equipment in equipments"> 
    	<a ng-href = "#/{{equipment.type === '空调' ? 'acController': 'lightController'}}/{{equipment.id}}">
    		<div class = "equipmentdetailrow" ng-class="{'equipmentOffColor': equipment.poweron === false} ">
	    		<div class="large">
	    			{{equipment.type}} - {{equipment.location}}
	    			<div class="small">
	    				<p>电源：{{equipment.poweron ? '开': '关'}}</p>
	    				<p ng-show="equipment.temperature">温度：{{equipment.temperature}} ℃ <img src='pic/Temp-sun-small.png' ng-show="equipment.heating"><img src='pic/Temp-snow-small.png' ng-show="!equipment.heating"></p>
	    				<p ng-show="equipment.moisture">湿度：{{equipment.moisture}} </p>
	    				<p ng-show="equipment.wind">风力：{{equipment.wind}} </p>
	    			</div>
	    		</div>
    		</div>
    	</a>
    </div>
 
 <!-- $.each(data.device, function (i, val) { 
            var div = $("<div/>").css("padding-left","20px");
            var line1 = $("<div/>").addClass("large");
            line1.html(val.type + '-' + val.location);
            div.append(line1);
            for(var attribute in val){
                var line = $("<div/>").addClass("small");
                if(attribute=="poweron"){
                    line.html("电源 : " + ((val[attribute]) ? "开" : "关"));
                    if (!val[attribute]) div.css("background-color", "#b3b3b3")
                }else if(attribute=="temperature"){
                    line.html("温度 : "+val[attribute]+"℃ | "+((val['heating'])?"<img src='Pic/Temp-sun-small.png'>":"<img src='Pic/Temp-snow-small.png'>"));
                }else if(attribute=="moisture"){
                    line.html("湿度 : "+val[attribute]);
                }else if(attribute=="wind"){
                    line.html("风力 : "+val[attribute]);
                } else if (attribute == "id") {
                    div.click(function () {
                        if(val['poweron']) window.location.href = "http://www.baidu.com" //val[attribute]; // href to the detail of the device
                    });
                }
                if(line.html()!="") div.append(line);
            }
            div.append("<br><hr>");
            $("#container").append(div);
        }); */-->


